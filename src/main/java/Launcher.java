
import javafx.application.Application;

/**
 * A launcher class to workaround classpath issues. (From the FX tutorial)
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}