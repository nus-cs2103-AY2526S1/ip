package gloqi.command;

/**
 * Represents the types of commands supported by the Gloqi chatbot.
 * Each enum constant corresponds to a specific user command.
 */
public enum Command {
    LIST,
    MARK,
    UNMARK,
    BYE,
    TODO,
    DEADLINE,
    EVENT,
    DELETE,
    SHOW,
    FIND,
    INVALID
}
