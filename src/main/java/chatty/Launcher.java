package chatty;

import chatty.ui.Main;
import javafx.application.Application;

/** Entry point used by Gradle to launch JavaFX. */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
